//Declaração de Funções
function digaOla(nome: string): void {
  console.log(`Olá, ${nome}!`);
}

const digaOla2 = (nome: string): void => {
  console.log(`Olá, ${nome}!`);
};

//Parâmetros e Tipos
function adicionar(a: number, b: number): number {
  return a + b;
}

//Valor de Retorno
function soma(a: number, b: number): number {
  return a + b;
}
